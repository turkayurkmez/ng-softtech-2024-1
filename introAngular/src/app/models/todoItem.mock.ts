import { TodoItem } from "./todoItem.model";

export const todoItems: TodoItem[] = [
    new TodoItem('Ekmek al', false),
    new TodoItem('.NET Core ile API geliştir', true),
    new TodoItem('Angular öğren', false)
];